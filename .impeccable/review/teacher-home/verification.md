# Teacher Home: scope and verification

User evidence: the supplied screenshot rejects the crowded teacher Home, duplicate Arabic curriculum buttons in English mode, and rows of published fixture content. The user screenshot is before evidence only.

Changed Home from a public catalog to the authenticated teacher's four most recent activities, a single primary Create activity entry and a narrow classroom-tools column. My activities is a searchable, status-filtered owned library with readable rows. Creation has a dedicated page collecting title, subject and stage, followed by the existing editor; teaching purpose remains available under More settings. Header action matches the same route, and the sidebar retains five primary destinations with secondary tools in a disclosure.

Shared discovery lives at /teacher/discover; /teacher/shelf redirects there. It has subject/stage selects, an optional priority-unit select rather than a repeated colored strip, larger activity previews and explicit copy actions. No hardcoded Math/Intermediate 2 first choice is imposed without saved preferences. Missing English unit translations use Unit + the stored order, preserving authored titles in their original language with bidi handling. Reference failures, empty searches, loading, draft/published states and failed preference saves have recovery controls.

Backend migration 0018 marks confirmed legacy helper and synthetic identities' activities/units discovery_hidden, preserving records, authors, published versions and history. Read-only verification on the configured application database confirmed 337 hidden activities and 24 hidden units, with zero remaining visible shared units. Other records were not classified by guesses about their titles. Owner listing and existing delivery authorization are preserved. The test bootstrap requires a named test database instead of defaulting to the SSH-forwarded application database. CI provisions asasera_test; manual transport checks need an explicit dedicated test API.

Validation: 52 targeted tests passed: 26 activity authoring/publication/authorization, 5 labels/discovery/cursor, 4 new fixture-discovery isolation and test-database guard, and 17 frontend unit-label/login-return. Backend/frontend builds and build assertions passed; backend typecheck, scoped frontend lint and whitespace checks passed. Schema snapshot regenerated from migration-only native PostgreSQL 16. The build retains the existing large game-renderer bundle warning. Static detector found 14 advisory local font-size/radius differences, no non-advisory findings. No generated or newly sourced rasters in this change; the existing theme thumbnails are reused.

Browser validation: blocked by the established admin-enforced browser security denial. No alternate browser, headless capture, URL alias or indirect rendering workaround was used. No current desktop/mobile or EN/AR screenshot has been captured, so rendered appearance, actual overflow, focus interaction and responsive presentation remain unverified. A fresh Impeccable reviewer must use recapture rather than claim visual approval. Source checks and passing tests do not close that limitation.
